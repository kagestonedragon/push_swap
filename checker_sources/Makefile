########################################################
#@@@-------------GENERATED BY AUTO_MAKE-------------@@@#
#@@                                                  @@#
#@    https://github.com/kagestonedragon/auto_make    @#
########################################################

NAME = checker

GCC = gcc

GCC_FLAGS = -Wall -Wextra -Werror

OBJ_PATH = objects/

.._INCLUDES_INCLUDE = ../includes/
.._INCLUDES_FT_PRINTF = ../includes/ft_printf/

._PATH = ./
.._PUSH_SWAP_VALIDATION_PATH = ../sources/validation/
.._PUSH_SWAP_ALGORITHM_PATH = ../sources/algorithm/
.._PUSH_SWAP_INITIALIZATION_PATH = ../sources/initialization/
.._PUSH_SWAP_OPERATIONS_PATH = ../sources/operations/
.._PUSH_SWAP_ERRORS_PATH = ../sources/errors/
.._PUSH_SWAP_FREE_PATH = ../sources/free/

._FILES = \
		main.c \
		get_next_line.c
.._PUSH_SWAP_INITIALIZATION_FILES = \
        get_elements.c \
		initialization.c \
    	new_list.c
.._PUSH_SWAP_OPERATIONS_FILES = \
		push.c \
		reverse_rotate.c \
		rotate.c \
		swap.c
.._PUSH_SWAP_ERRORS_FILES = \
		push_swap_log.c
.._PUSH_SWAP_VALIDATION_FILES = \
        validation.c
.._PUSH_SWAP_FREE_FILES = \
        push_swap_free.c
.._PUSH_SWAP_ALGORITHM_FILES = \
        primitive_sorting.c \
        add_operation.c



OBJ_._CORE = $(addprefix $(OBJ_PATH), $(._FILES:.c=.o))
OBJ_.._PUSH_SWAP_INITIALIZATION_CORE = $(addprefix $(OBJ_PATH), $(.._PUSH_SWAP_INITIALIZATION_FILES:.c=.o))
OBJ_.._PUSH_SWAP_OPERATIONS_CORE = $(addprefix $(OBJ_PATH), $(.._PUSH_SWAP_OPERATIONS_FILES:.c=.o))
OBJ_.._PUSH_SWAP_ERRORS_CORE = $(addprefix $(OBJ_PATH), $(.._PUSH_SWAP_ERRORS_FILES:.c=.o))
OBJ_.._PUSH_SWAP_VALIDATION_CORE = $(addprefix $(OBJ_PATH), $(.._PUSH_SWAP_VALIDATION_FILES:.c=.o))
OBJ_.._PUSH_SWAP_FREE_CORE = $(addprefix $(OBJ_PATH), $(.._PUSH_SWAP_FREE_FILES:.c=.o))
OBJ_.._PUSH_SWAP_ALGORITHM_CORE = $(addprefix $(OBJ_PATH), $(.._PUSH_SWAP_ALGORITHM_FILES:.c=.o))
OBJECTS = $(OBJ_._CORE) $(OBJ_.._PUSH_SWAP_INITIALIZATION_CORE) $(OBJ_.._PUSH_SWAP_OPERATIONS_CORE) $(OBJ_.._PUSH_SWAP_ERRORS_CORE) $(OBJ_.._PUSH_SWAP_VALIDATION_CORE) $(OBJ_.._PUSH_SWAP_FREE_CORE) $(OBJ_.._PUSH_SWAP_ALGORITHM_CORE)

all: $(NAME)

$(NAME): $(OBJECTS)
		@($(GCC) $(GCC_FLAGS) $(OBJECTS) -o $(NAME) -L ../libraries -lft -L ../libraries -lftprintf)
		@(mv checker ../)
		@(echo "[0;92m"$(NAME) "was created![0;0m")

clean:
		@(/bin/rm -rf $(OBJ_PATH))
		@(echo "[0;91mAll object files have been deleted![0;0m")

fclean: clean
		@(/bin/rm -f $(NAME))
		@(echo "[0;91m"$(NAME) "has been deleted![0;0m")

re: fclean all

objects:
		@(mkdir $(OBJ_PATH))

objects/%.o: $(._PATH)%.c | objects
		@($(GCC) $(GCC_FLAGS) -I $(.._INCLUDES_INCLUDE) -I $(.._INCLUDES_FT_PRINTF) -c $< -o $@)
		@(echo "[0;93m"$< "->" $@"[0;0m")

objects/%.o: $(.._PUSH_SWAP_INITIALIZATION_PATH)%.c | objects
		@($(GCC) $(GCC_FLAGS) -I $(.._INCLUDES_INCLUDE) -c $< -o $@)
		@(echo "[0;93m"$< "->" $@"[0;0m")

objects/%.o: $(.._PUSH_SWAP_OPERATIONS_PATH)%.c | objects
		@($(GCC) $(GCC_FLAGS) -I $(.._INCLUDES_INCLUDE) -c $< -o $@)
		@(echo "[0;93m"$< "->" $@"[0;0m")

objects/%.o: $(.._PUSH_SWAP_ERRORS_PATH)%.c | objects
		@($(GCC) $(GCC_FLAGS) -I $(.._INCLUDES_INCLUDE) -c $< -o $@)
		@(echo "[0;93m"$< "->" $@"[0;0m")

objects/%.o: $(.._PUSH_SWAP_VALIDATION_PATH)%.c | objects
		@($(GCC) $(GCC_FLAGS) -I $(.._INCLUDES_INCLUDE) -c $< -o $@)
		@(echo "[0;93m"$< "->" $@"[0;0m")

objects/%.o: $(.._PUSH_SWAP_FREE_PATH)%.c | objects
		@($(GCC) $(GCC_FLAGS) -I $(.._INCLUDES_INCLUDE) -c $< -o $@)
		@(echo "[0;93m"$< "->" $@"[0;0m")

objects/%.o: $(.._PUSH_SWAP_ALGORITHM_PATH)%.c | objects
		@($(GCC) $(GCC_FLAGS) -I $(.._INCLUDES_INCLUDE) -c $< -o $@)
		@(echo "[0;93m"$< "->" $@"[0;0m")
