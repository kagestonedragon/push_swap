#include "../../includes/push_swap.h"
#include "../../includes/errors.h"

int         check_elements(t_list *list_a)
{
    if (!list_a || !list_a->next)
        return (ERROR_EMPTY);
}

int         flag_sa(t_list *list_a)
{
    t_list  *temp;

    t_list 
}
